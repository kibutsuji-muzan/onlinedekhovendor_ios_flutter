import 'package:flutter/material.dart';

class coming_soon extends StatelessWidget {
  const coming_soon({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("COMING SOON",style: TextStyle(fontSize: 30),),
    );
  }
}
